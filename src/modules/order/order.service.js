const orderService = {
  getAll: async (params) => {},
  getDetail: async (params) => {},
  create: async (params) => {},
  getHistory: async (params) => {},
  cancel: async (params) => {},
  report: async (params) => {},
};

export default orderService;
